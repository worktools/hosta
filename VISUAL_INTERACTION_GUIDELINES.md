# Hosta 视觉与交互规范

> 状态：Active
>
> 目的：为 Hosta 的界面改进和新功能开发提供一致、可验证的设计约束。本文优先于临时的局部视觉偏好；实现细节以 [frontend/src/styles.css](frontend/src/styles.css) 中的 token 为准。

## 1. 产品界面定位

Hosta 是一个面向程序创建、验证、发布和运行观测的本地优先开发工作台，不是营销站点，也不是通用内容管理系统。

界面应当帮助用户持续回答四个问题：

1. 我正在查看哪个程序、哪个版本？
2. 这个版本是否可运行、可测试、可发布？
3. 数据、输入输出契约和运行结果是否可信？
4. 下一步最合适的操作是什么？

因此，整体体验应保持克制、信息密度适中、可快速扫读。代码、Schema、日志和结构化数据是一级内容，不应被装饰性卡片或宣传式版面稀释。

## 2. 设计原则

### 2.1 流程优先

以“创建 → 定义契约 → 编辑/生成 → 测试 → 发布 → 观测”为主线组织页面和动作。每个页面只突出一个主操作；删除、恢复、清空等不可逆操作保持次级并要求确认。

### 2.2 结构先于装饰

使用分组、标题、边界、间距和排版传达层级。禁止为填充页面而添加大幅 hero、渐变装饰、悬浮卡片墙或无语义插图。

### 2.3 契约可见

输入 Schema、输出 Schema、外部数据源的输入输出 Schema、测试输入及预期输出，都属于程序的可执行文档。它们应在相邻工作区中可见、可编辑、可从测试推断，并在保存后明确反馈状态。

### 2.4 状态不只靠颜色

状态至少由颜色加文字共同表达：例如“ready”“needs_revision”“已应用”“待确认”。成功、警告、失败和信息色不得作为唯一信号。

### 2.5 先验证再推进

生成、保存、迁移、发布、外部接口调用等异步操作必须提供进行中、成功和失败反馈。发布与数据替换必须显式确认；失败信息应说明对象、原因和下一步，而不只显示“请求失败”。

## 3. 视觉基础

### 3.1 主题与色彩

采用 Carbon G10 Light：白色工作层、浅灰页面背景、IBM 蓝作为唯一主交互色。所有新颜色优先复用 `--cds-*` token，不直接写十六进制值；以下是语义映射：

| 语义      | Token                                         | 使用范围                         |
| --------- | --------------------------------------------- | -------------------------------- |
| 页面背景  | `--cds-bg-secondary`                          | 应用工作区背景                   |
| 表层/面板 | `--cds-layer`                                 | 编辑器外围、列表、表单和数据面板 |
| 默认文本  | `--cds-text-primary`                          | 标题、正文、关键数值             |
| 次级文本  | `--cds-text-secondary`                        | 描述、元信息、辅助提示           |
| 主交互    | `--cds-button-primary` / `--cds-link`         | 主按钮、选中指示、可点击文本     |
| 悬停高亮  | `--cds-highlight`                             | 导航与标签的 hover 背景          |
| 成功      | `--cds-support-success`                       | 已就绪、通过、已发布             |
| 警告      | `--cds-support-warning`                       | 待确认、需人工关注               |
| 错误/危险 | `--cds-support-error` / `--cds-button-danger` | 失败、删除、校验错误             |

禁止用紫色渐变、无语义的彩色背景或大面积状态色作为装饰。状态色的浅色版本只用于提示区、轻量标记和 hover，不用于大面积页面底色。

### 3.2 排版

- UI 正文使用 IBM Plex Sans；代码、JSON、ID、URL、HTTP 方法、日志使用 IBM Plex Mono。
- 页面标题使用轻字重（`300`）和紧凑尺寸；面板标题使用 `600` 字重的小字号大写/标签风格。
- 正文默认 `0.875rem`；辅助文本 `0.75rem`；代码 `0.6875rem` 至 `0.8125rem`。
- 不使用 viewport 单位缩放字体、不使用负字距、不为了“视觉冲击”放大工作台内标题。

### 3.3 间距、边界与圆角

- 仅使用 2px 网格 token：`--sp-01` 到 `--sp-13`。
- 组件内部通常使用 `--sp-03` 至 `--sp-05`，页面与主要区块使用 `--sp-06` 至 `--sp-07`。
- 表层使用 1px `--cds-border-subtle` 分隔；活动输入与键盘焦点使用 `--cds-border-interactive`。
- Hosta 使用平直工作台风格：默认不添加圆角。只有确有局部状态意义的标记可以使用小圆角。

### 3.4 代码与结构化数据

- 可编辑代码、JSON Schema 和数据源内容一律使用深色代码面：`--code-bg`、`--code-text`。
- 运行输出、快照预览和日志可使用浅灰只读面；内容必须可复制、可滚动、可换行。
- 不把 JSON/日志塞进普通段落或 tooltip。内容较长时保留稳定高度和独立滚动。

## 4. 信息架构与布局

### 4.1 应用壳

- 左侧栏用于跨程序导航、应用切换和全局入口；桌面宽度保持 256px。
- 主内容区按“上下文头部 → 当前页签 → 工作区”排列。
- 头部必须展示当前应用/版本上下文和高价值状态；操作按钮按危险、次要、主要顺序从左到右组织，发布是唯一主操作。

### 4.2 页签

页签用于同一对象内的平级工作区，而不是页面流程的进度条。

- 默认态：透明背景、次级文本、无下划线。
- Hover：`--cds-highlight` 背景加 `--cds-support-info-dim` 下划线；禁止灰色 hover 下划线。
- Active：默认背景、主文本、2px `--cds-border-interactive` 下划线、`600` 字重。
- 页签名称使用名词，数量仅用于有实际数量价值的集合，例如“测试用例 (9)”。
- 子页签使用同一模式，避免发明第二套胶囊按钮或分段控件视觉。

### 4.3 工作区布局

- 对比型任务采用双栏：代码/运行、输入/输出 Schema、列表/详情。
- 左侧优先放主对象或可编辑内容；右侧放运行、辅助配置、详情或历史。
- 数据源页面中“内部状态”与“外部数据”是不同来源，使用子页签区分；外部数据采用“列表 + 配置详情”布局，未来可无缝扩展为选择或新增来源。
- 页面区块不应包裹在嵌套卡片中。只有重复条目、模态层和独立工具面板才可以有边框容器。

## 5. 组件规范

### 5.1 导航

- 侧栏 hover 使用 `--cds-highlight`，选中项使用 `--cds-layer-selected` 加左侧 3px 蓝色指示条。
- 导航名称保持简短且稳定；“最近程序”可显示运行状态圆点，但名称始终是主要信息。
- `:focus-visible` 必须存在，键盘焦点不可仅依赖 hover。

### 5.2 按钮与命令

| 类型     | 样式             | 用途                                               |
| -------- | ---------------- | -------------------------------------------------- |
| 主按钮   | 蓝底白字         | 页面唯一的下一步或提交动作，如发布、保存新增数据源 |
| 次要按钮 | 边框/透明        | 格式化、测试、取消、刷新等可逆辅助动作             |
| 文字按钮 | 无容器，蓝色文本 | 行内操作、列表编辑、恢复、复制                     |
| 危险按钮 | 红色语义         | 删除、清空、停用；执行前确认                       |
| 图标按钮 | 图标加 tooltip   | 紧凑工具栏中的明确通用命令                         |

- 同一视口内不应出现多个无差别的蓝色主按钮。
- 运行、生成、保存等异步动作进行中必须禁用并显示“运行中…”等动词状态。
- 不使用 emoji 作为唯一图标语义；新建按钮优先用文本或后续引入统一图标库。

### 5.3 输入与表单

- 使用下划线输入框，不添加厚重矩形边框。
- Label 始终位于输入上方，placeholder 只提供示例，不替代 label。
- 校验错误紧邻对应字段，说明如何修正；保存错误同时通过 toast 提醒。
- Schema、Headers、请求体等 JSON 字段标明格式，并在保存前本地解析。

### 5.4 列表与表格

- 列表条目展示一行主信息、一行必要的次级信息和有限行内操作。
- 选中列表项以淡蓝背景和蓝色左边界表示；hover 不能与选中态混淆。
- 表格用于需要跨行比较的运行记录和统计；列必须包含稳定的名称，数值列对齐规则一致。
- 空状态说明当前没有什么、会在什么操作后出现，不使用插画占位。

### 5.5 通知、确认与错误

- Toast 用于短暂的成功、完成和网络错误提示；正文要包含操作对象，例如“外部数据源已添加”。
- 字段级错误保留到用户修正；运行、迁移和接口测试错误保留在结果区并展示原始可读信息。
- 删除、恢复快照、应用迁移、清空 Schema 等影响数据的动作必须确认，确认文案明确后果。

## 6. 关键流程规范

### 6.1 创建与生成

创建页只收集生成所需的最少字段。生成中保留用户输入，不跳转到空白页面；完成后定位到新程序的需求或代码工作区，并用状态提示结果。

### 6.2 契约与测试

- 输入 Schema 用于测试输入和运行请求校验。
- 输出 Schema 作为测试断言和接口文档的结构依据；后续实现运行时输出校验时，错误应包含字段路径和实际值。
- “从测试推断”只填充编辑器，不应悄悄覆盖已保存内容；用户必须通过保存明确提交。

### 6.3 数据源

- 内部状态是程序维护的 JSON 数据，具备保存、快照、迁移、恢复能力。
- 外部数据源是具名 HTTP 接口；一个接口对应一个数据源，可配置 URL、方法、Headers 和输入输出 Schema。
- 外部数据源测试必须展示 HTTP 状态、耗时和响应体；不得把密钥、Authorization Header 写进运行日志或截图。
- 未来“选择数据源”与“添加数据源”沿用同一列表模型，不在代码编辑器中插入无结构的 URL 字符串。

### 6.4 发布与观测

- 发布前明确展示版本状态和最近测试结果；不可发布时说明阻塞原因。
- 运行记录要保留版本、数据源快照/引用、状态、耗时、触发方式和日志入口。
- 失败优先显示错误摘要和可操作入口，再显示原始技术细节。

## 7. 响应式与可访问性

- 850px 以下隐藏侧栏，主工作区改为单列；双栏 Schema、数据源和编辑器布局必须垂直堆叠。
- 页签可横向滚动，不截断当前选中项。
- 点击目标最小高度为 40px；密集的文字按钮可例外，但相邻目标之间保持足够间距。
- 所有可操作元素有 `:focus-visible` 样式，所有输入有可见 label。
- 禁止仅通过颜色区分状态；文本对比度至少满足正常正文可读性。

## 8. 开发验收清单

新增或改造界面前后，逐项检查：

- [ ] 是否服务于创建、定义、验证、发布或观测中的一个明确步骤？
- [ ] 是否使用了已有 Carbon token、间距 token 和已有组件模式？
- [ ] 页面是否只有一个主操作？危险操作是否有确认？
- [ ] Hover、active、disabled、loading、empty、error 和 keyboard focus 状态是否完整？
- [ ] 长文本、URL、JSON、错误堆栈和移动端是否会溢出或遮挡？
- [ ] 新增状态是否同时有文字与颜色表达？
- [ ] 是否保留了能让用户理解来源、版本、时间和结果的关键信息？
- [ ] 是否执行了 `npm run build`？

## 9. 实现约定

- CSS token 集中定义在 `frontend/src/styles.css` 的 `:root`；新增 token 必须说明语义，禁止为单一组件创建颜色变量。
- 组件 class 保持语义化并按功能前缀命名，例如 `ext-ds-*`、`schema-*`，避免以布局或颜色命名。
- 复杂异步状态优先在 JSX 中显式建模，而不是仅依赖 CSS 隐藏。
- 当修改全局交互样式（导航、按钮、页签、表单）时，同步更新本文相关章节，并进行桌面与窄屏验证。
